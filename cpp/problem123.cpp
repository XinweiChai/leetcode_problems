#include <iostream>
#include <vector>
using namespace std;

class Solution {
   public:
    int maxProfit(vector<int>& prices) {}

    int chooseK(vector<int>& prices, int k) {

    }
};

int main(int argc, char const* argv[]) {
    vector<int> a = {3, 3, 5, 0, 0, 3, 1, 4};
    // cout << Solution().maxProfit(a,1);
    cout << Solution().chooseK(a, 1)<<endl;
    return 0;
}
